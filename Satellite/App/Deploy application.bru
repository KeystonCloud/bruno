meta {
  name: Deploy application
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/app/deploy
  body: json
  auth: inherit
}

body:json {
  {
    "team_id": "{{team_id}}",
    "name": "my-app",
    "content": "Hello world!"
  }
}

body:multipart-form {
  name: my-app
}

settings {
  encodeUrl: true
  timeout: 0
}
