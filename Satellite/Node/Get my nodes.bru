meta {
  name: Get my nodes
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/node/mine
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
