meta {
  name: Heartbeat
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/node/heartbeat
  body: json
  auth: inherit
}

body:json {
  {
    "id": "{{node_id}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
