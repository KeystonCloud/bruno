meta {
  name: Register new node
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/node/register
  body: json
  auth: inherit
}

body:json {
  {
    "owner_id": "{{team_uuid}}",
    "name": "my_test_node",
    "port": 9000
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
