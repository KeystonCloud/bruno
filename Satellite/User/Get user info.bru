meta {
  name: Get user info
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/user/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
