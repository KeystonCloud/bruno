meta {
  name: List all users
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/user
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
