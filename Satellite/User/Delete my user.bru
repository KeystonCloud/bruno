meta {
  name: Delete my user
  type: http
  seq: 9
}

delete {
  url: {{base_url}}/api/user/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
