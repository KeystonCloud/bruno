meta {
  name: Get my apps
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/app/mine
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
