meta {
  name: Update user info
  type: http
  seq: 5
}

put {
  url: {{base_url}}/api/user/{{user_uuid}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Gregory House",
    "email": "ghouse@ppth.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
