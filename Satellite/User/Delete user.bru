meta {
  name: Delete user
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/user/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
