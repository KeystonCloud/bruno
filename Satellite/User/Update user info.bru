meta {
  name: Update user info
  type: http
  seq: 4
}

put {
  url: {{base_url}}/api/user/{{user_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Lisa Cuddy",
    "email": "lcuddy@ppth.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
