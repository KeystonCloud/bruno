meta {
  name: Update my user info
  type: http
  seq: 8
}

put {
  url: {{base_url}}/api/user/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Gregory House",
    "email": "ghouse@ppth.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
